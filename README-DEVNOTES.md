Developer notes:
- Replace placeholders in `src/components` with real content and images.
- Tailwind config is minimal; extend as needed.
- The mock API runs on port 4000. Vite is proxied to it for `/api` calls.
